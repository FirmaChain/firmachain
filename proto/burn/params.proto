syntax = "proto3";
package firmachain.firmachain.burn;

import "gogoproto/gogo.proto";

option go_package = "github.com/firmachain/firmachain/x/burn/types";

// Params defines the parameters for the module.
message Params {
  option (gogoproto.goproto_stringer) = false;
  
}